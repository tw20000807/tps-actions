name: Build tests

on:
  push:
    branches:
      - '**'
    paths:
      - '*/gen/**'
      - '*/solution/**'
      - '*/validator/**'
      - '*/problem.json'
      - '*/solutions.json'
      - '*/subtasks.json'
  workflow_dispatch:
    inputs:
      input:
        description: 'Problem name to build inputs'
        default: ''
      output:
        description: 'Problem name to build outputs'
        default: ''
      solutions:
        description: 'Problem name to run solutions'
        default: ''

jobs:
  run:
    uses: tw20000807/tps-actions/.github/workflows/build-tests.yml@main
    with:
      input: ${{ github.event.inputs.input }}
      output: ${{ github.event.inputs.output }}
      solutions: ${{ github.event.inputs.solutions }}
