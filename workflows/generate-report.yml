name: Generate report

on:
  push:
    branches:
      - '**'
    paths:
      - 'p*/**'
  workflow_dispatch:

jobs:
  run:
    uses: tw20000807/tps-actions/.github/workflows/generate-report.yml@main
